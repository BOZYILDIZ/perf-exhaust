import OfflineScreen from "./OfflineScreen";

export const metadata = { title: "Hors connexion — PERF'EXHAUST Admin", robots: { index: false, follow: false } };

export default function AdminOfflinePage() {
  return <OfflineScreen />;
}
